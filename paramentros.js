function parametro(y, x) {
    const resultado = y + x;
    return resultado;
} 
var resultadoDaFuncao = parametro(12, 10);



// apenas um ajuste pessoal
const soma = (args0, args1) =>
{
    return args0 + args1;
}

const somaDosParametros = soma(5, "Beterraba");
console.log(somaDosParametros);

// nesse caso, a soma nao quer dizer que é apenas uma operação matematica, pode ser a soma de duas strings, que seria a concatenação
